FROM node:20-alpine

WORKDIR /app

# Install build dependencies for sqlite3 compile if necessary
RUN apk add --no-cache python3 make g++

COPY server/package*.json ./
RUN npm install --production

COPY server/ ./

EXPOSE 3000

CMD ["npm", "start"]
